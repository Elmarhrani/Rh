{% extends '::base.html.twig' %}

{% block stylesheets %}
    <link href="{{ asset('assets/css/formStyle.css') }}" rel="stylesheet" type="text/css"/>
{% endblock %}

{% block QUICKVIEW %}  {% endblock %}

{% block CONTENT %}
    <div class="jumbotron" data-pages="parallax">
        <div class="container-fluid container-fixed-lg sm-p-l-20 sm-p-r-20">
            <div class="inner">
                <!-- START BREADCRUMB -->
                {#{% block BREADCRUMB %}#}
                {#<ul class="breadcrumb">#}
                {#<li>#}
                {#<a href="#">Accueil</a>#}
                {#</li>#}
                {#</ul>#}
                {#{% endblock BREADCRUMB %}#}
                <!-- END BREADCRUMB -->
            </div>
        </div>
    </div>
    <!-- END JUMBOTRON -->
    <!-- START CONTAINER FLUID -->
    <div class="container-fluid container-fixed-lg">
        <div class="row">
            <div class="col-md-8 col-md-offset-2">
                <!-- START PANEL -->
                <div class="panel panel-transparent">
                    <div class="panel-heading ">
                        <div class="panel-title">
                            <div>
                                {% block form_title %} form_title {% endblock %}
                            </div>
                        </div>
                    </div>
                    <div class="panel-body">
                        <div>
{#                            {% if form is defined %}
                                {% form_theme form 'Form/fields.html.twig' %}
                            {% elseif edit_form is defined %}
                                {% form_theme edit_form 'Form/fields.html.twig' %}
                            {% endif %}#}
                            {% block form_content %}
                                form_content
                            {% endblock form_content %}
                        </div>
                    </div>
                </div>
                <!-- END PANEL -->
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}


    <script src="{{ asset('js/myFormElems.js') }}" type="text/javascript"></script>

    <script src="{{ asset('pages/js/pages.min.js') }}" type="text/javascript"></script>
    <script src="{{ asset('assets/js/form_elements.js') }}" type="text/javascript"></script>
    <script src="{{ asset('assets/js/scripts.js') }}" type="text/javascript"></script>
{% endblock %}
